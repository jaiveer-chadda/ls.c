/// @file features/types/types.h

#ifndef GET_TYPE_INITIALISED
#define GET_TYPE_INITIALISED

#include "../../model/stat-model.h"

void getType(const path_t file_path);

#endif /* !GET_TYPE_INITIALISED */
