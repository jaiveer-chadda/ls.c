/// @file parsing/parse-file.c

#include "parse-file.h"

/**
 * @brief Convert the information in `FileStat` from raw data into formatted, displayable output.
 *
 * @param pfile A pointer to a `FileStat` object, where the raw information for a given file is stored
 * @return The original pointer to `pfile`.
 */
FileStat *parseFile(FileStat *const pfile) {
	(void) pfile;
	return pfile;
}
